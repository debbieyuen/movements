# python
.venv/
__pycache__/
*.pyc
*.egg-info/

# capture + processing data (never committed; clips are shared out-of-band)
sessions/
live/
dataset/
inbox/
received_frames/

# pinned external clones on the GPU box (see docs/GPU_SETUP.md)
external/

.DS_Store

import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* To open the dev server from phones on your LAN, allow your machine's IP:
     allowedDevOrigins: ['192.168.x.x'],  */
};

export default nextConfig;